package me.dejawu.kythera.frontend.node;

public enum NodeKind {
    ACCESS,
    AS,
    ASSIGN,
    BINARY,
    BLOCK,
    CALL,
    IDENTIFIER,
    IF,
    LET,
    LITERAL,
    RETURN,
    TYPEOF,
    UNARY,
    WHILE,
}
