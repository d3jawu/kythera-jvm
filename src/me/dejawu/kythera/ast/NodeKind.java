package me.dejawu.kythera.ast;

public enum NodeKind {
    ACCESS,
    ASSIGN,
    BINARY,
    BLOCK,
    CALL,
    IDENTIFIER,
    IF,
    LET,
    LITERAL,
    RETURN,
    TYPEOF,
    UNARY,
    WHILE,
}
