package me.dejawu.kythera.ast;

public enum NodeKind {
    ACCESS,
    ASSIGN,
    BINARY,
    BLOCK,
    CALL,
    IDENTIFIER,
    IF,
    LET,
    CONST,
    LITERAL,
    RETURN,
    TYPEOF,
    UNARY,
    WHILE,
}
