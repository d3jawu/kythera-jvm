package io.kwu.kythera.frontend.node;

public enum NodeKind {
    UNARY,
    BINARY,
    ASSIGN,
    LITERAL,
    IDENTIFIER,
    LET,
    IF,
    WHILE,
    RETURN,
    AS,
    TYPEOF,
    CALL,
    ACCESS,
    BLOCK,
}
